# Base URL of the FastAPI backend this frontend talks to.
# Local development default: http://localhost:8000
# On Vercel set this as a project environment variable (Production + Preview)
# to your Render service URL, e.g. https://credit-scoring-api.onrender.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
